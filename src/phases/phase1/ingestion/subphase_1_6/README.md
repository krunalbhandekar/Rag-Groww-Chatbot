# Subphase 1.6 — Provenance manifest

**Status:** scaffolded (not implemented).

**Planned:** Per-URL / per-snapshot sidecar JSON for Phase 2 (`scheme_id`, `snapshot_id`, hashes, HTTP metadata, parser version).
